name: Auto Merge Main to Testing Branch

on:
  push:
    branches:
      - main

jobs:
  merge:
    runs-on: ubuntu-latest

    steps:
    - name: Check out the repo
      uses: actions/checkout@v3

    - name: Configure Git
      run: |
        git config --global user.name 'KrishnaVeni782'
        git config --global user.email 'chinnidayaka@gmail.com'

    - name: Fetch all branches
      run: git fetch --all

    - name: Merge main into testing
      run: |
        git checkout testing
        git merge  -X theirs main --allow-unrelated-histories
        git push origin testing